// AUTO-GENERATED by scripts/generate-registry.ts — do not edit manually.


import imageCompressManifest from '@/tools/image/image-compress/manifest';
import imageCropManifest from '@/tools/image/image-crop/manifest';
import imageFormatConvertManifest from '@/tools/image/image-format-convert/manifest';
import imageResizeManifest from '@/tools/image/image-resize/manifest';
import imageRotateManifest from '@/tools/image/image-rotate/manifest';

import type { ToolManifest } from "@packages/types";

export const allManifests: ToolManifest[] = [
  imageCompressManifest,
  imageCropManifest,
  imageFormatConvertManifest,
  imageResizeManifest,
  imageRotateManifest,
];

export const registry = {
  bySlug(slug: string): ToolManifest | undefined {
    return allManifests.find((m) => m.slug === slug);
  },
  byCategory(category: string): ToolManifest[] {
    return allManifests.filter((m) => m.category === category);
  },
  relatedTo(slug: string): ToolManifest[] {
    const tool = allManifests.find((m) => m.slug === slug);
    if (!tool) return [];
    return tool.relatedTools
      .map((s) => allManifests.find((m) => m.slug === s))
      .filter((m): m is ToolManifest => Boolean(m));
  },
  all(): ToolManifest[] {
    return allManifests;
  },
};
